0. 키와 값을 쌍으로 데이터로 가지는 자료형
데이터 조회 및 수정 O(1) 시간복잡도

1. 사용법
data = dict()
data['사과'] = 'Apple'
print(data)
if '사과' in data:
    print("'사과'를 키로 가지는 데이터가 존재합니다.")

또는 아래와 같이 초기화
b = { 'a' : 1, 'b' : 2 }

2. 메서드
keys() : 키 데이터만 뽑아서 리스트로 사용
values() : 값 데이터만 뽑아서 리스트로 사용
print(data.values())

ex)
# 각 키 에 따른 값을 하나씩 출력
for key in key_list:
    print(data[key])

